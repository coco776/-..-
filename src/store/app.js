const state = {
    device:"desktop"
}
const actions = {
//响应
}
const mutations = {
    SET_DEVICE(state, data){
        state.device = data;
    }
}
export default {
    state,
    actions,
    mutations
}